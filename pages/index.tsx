import type { NextPage } from "next";
import Nav from "../components/Nav";
import Header from "../components/Header";
const Home: NextPage = () => {
  return (
    <div>
      <Nav />
      <Header />
    </div>
  );
};

export default Home;
