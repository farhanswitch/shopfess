import { FC } from "react";

const TopProducts: FC = () => {
  return (
    <section className="top-products max-w-5xl mx-auto pt-6">
      <h2 className="text-center text-xl">Top Products</h2>
    </section>
  );
};

export default TopProducts;
